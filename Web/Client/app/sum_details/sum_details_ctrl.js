(function () {
    'use strict';

    angular
        .module('app.page')
        .controller('sum_details_ctrl', sum_details_ctrl);

    sum_details_ctrl.$inject = ['$scope'];

    function sum_details_ctrl(vm) {

    }
})();