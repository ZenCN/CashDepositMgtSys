(function () {
    'use strict';

    angular
        .module('app.layout')
        .controller('dashboard_ctrl', dashboard_ctrl);

    dashboard_ctrl.$inject = ['$scope',];

    function dashboard_ctrl(vm) {

    }
})();