(function () {
    'use strict';

    angular
        .module('app.page')
        .controller('gives_sum_details_ctrl', gives_sum_details_ctrl);

    gives_sum_details_ctrl.$inject = ['$scope'];

    function gives_sum_details_ctrl(vm) {

    }
})();