(function () {
    'use strict';

    angular
        .module('app.page')
        .controller('sys_settings_ctrl', sys_settings_ctrl);

    sys_settings_ctrl.$inject = ['$scope'];

    function sys_settings_ctrl(vm) {

    }
})();